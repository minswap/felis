version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_NETWORK_ENV: TESTNET_PREVIEW
      PORT: 3002
    expose:
      - "3002"
    restart: unless-stopped
